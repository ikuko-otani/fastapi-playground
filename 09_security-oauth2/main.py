# Chapter F-09: Security: OAuth2 with Password Bearer（触りのみ）
# 日本語訳：セキュリティ：OAuth2 パスワードベアラー（入門のみ）
# Ref: https://fastapi.tiangolo.com/tutorial/security/first-steps/
#
# Run: uvicorn main:app --reload
# 日本語訳：uvicorn main:app --reload で起動
# Docs: http://localhost:8000/docs

# TODO Step 1: Import OAuth2PasswordBearer and Depends
# 日本語訳：OAuth2PasswordBearer と Depends をインポートする

# TODO Step 2: Create OAuth2PasswordBearer instance with tokenUrl="token"
# 日本語訳：tokenUrl="token" を指定して OAuth2PasswordBearer インスタンスを作成する

# TODO Step 3: Create FastAPI app instance
# 日本語訳：FastAPI アプリインスタンスを作成する

# TODO Step 4: Define /items/ endpoint that depends on oauth2_scheme
# 日本語訳：oauth2_scheme を依存関係として持つ /items/ エンドポイントを定義する
